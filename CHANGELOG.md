# Changelog

## Unreleased

- Add Sprite's native Explorer with Material icons, Dark Modern colors, native
  docking and resize, mouse and Vim navigation, literal filename search,
  external refresh, and active-file reveal.
- Save tree selection, expansion, scroll and renderer-specific widths per root
  for the current Neovim session. Show dotfiles by default.
- Keep the terminal tree available in ordinary Neovim and as a fallback when
  Sprite's native dock is unavailable. Terminal icons retain their graphics and
  plain-text fallbacks and custom-pack behavior.
- Resolve Material's parent-qualified file and folder associations, including
  mixed-case keys, in the native tree.
